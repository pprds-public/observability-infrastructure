apiVersion: 1

providers:
  - name: 'dashboard-template'
    orgId: 1
    folder: 'DashboardTemplate'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 0   # 0 = disable auto update
    options:
      path: /etc/grafana/provisioning/dashboards

